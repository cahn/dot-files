#
# This is the config file, and
# a '#' or ';' character indicates
# a comment
#
; core variables
[core]
        excludesfile = ~/.gitignore
[user]
	name = Charles T. Ahn
	email = 
[branch]
	autosetupmerge = true
[color]
	diff = auto
	status = auto
	branch = auto
	interactive = auto
[push]
        default = upstream
[merge]
	tool = opendiff
	summary = true
